enum UrlType { image, video, unknown,}

enum PlayState { playing, paused, empty, completed, canceled, interrupted}

enum InputType { number, string, bool, image, unknown}

enum FilterType { top, recent }